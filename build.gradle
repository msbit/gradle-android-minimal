buildscript {
  repositories {
    google()
    mavenCentral()
  }

  dependencies {
    classpath group: 'com.android.tools.build', name: 'gradle', version: '7.3.0-alpha07'
  }
}

allprojects {
  repositories {
    google()
    mavenCentral()
  }
}
