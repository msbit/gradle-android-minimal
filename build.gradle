buildscript {
  repositories {
    google()
    mavenCentral()
  }

  dependencies {
    classpath group: 'com.android.tools.build', name: 'gradle', version: '7.0.0-beta02'
  }
}

allprojects {
  repositories {
    google()
    mavenCentral()
  }
}
